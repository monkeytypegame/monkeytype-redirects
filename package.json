{
  "name": "monkeytype-redirects",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd frontend && pnpm build && cd ../backend && pnpm start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "turbo run dev",
    "docker-build": "docker build --build-arg VITE_API_BASE_URL=$VITE_API_BASE_URL -t monkeytype-redirects-app:latest ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "turbo": "^2.5.3"
  },
  "packageManager": "pnpm@9.6.0"
}
